import React from "react";
import { View } from "react-native";
import { BOTTOM_NAVIGATION_FULL_HEIGHT } from "../constants/constants";

const ListFooter = () => {
  return <View style={{ height: BOTTOM_NAVIGATION_FULL_HEIGHT }} />;
};

export default ListFooter;
